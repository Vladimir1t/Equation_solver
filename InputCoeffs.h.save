#ifndef INPUTCOEFFS_H_INCLUDED
#define INPUTCOEFFS_H_INCLUDED
#include <assert.h>

double InputCoeffs(double* s)
{
    assert (s!=NULL);
    scanf("%lg", s);
    assert (isfinite(*s));
}

#endif // INPUTCOEFFS_H_INCLUDED
